// Factorial using single-seed stream generator
// fact[n] = n! (0-indexed, so fact[5] = 120)
fact = [1, ... i * x]

// Test
!fact[0]   // 1 (0!)
!fact[1]   // 1 (1!)
!fact[2]   // 2 (2!)
!fact[3]   // 6 (3!)
!fact[4]   // 24 (4!)
!fact[5]   // 120 (5!)
