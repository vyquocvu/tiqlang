// Struct types demonstration

struct Point {
    x: i64,
    y: i64
}

p = Point { x: 3, y: 4 }
print(p.x)
print(p.y)

// Structs in functions (with type annotation)
manhattan pt:Point -> i64 -> pt.x + pt.y

q = Point { x: 10, y: 20 }
print(manhattan(q))
