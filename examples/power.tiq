// Power: base^exp using stream generator
// Syntax: pow(base)[exp] returns base^exp
pow base -> [1, ... x * base]

// Test - bracket call syntax
!pow(2)[0]   // 1
!pow(2)[1]   // 2
!pow(2)[10]  // 1024
!pow(3)[4]   // 81
